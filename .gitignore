node_modules/
.expo/
dist/
*.log
.DS_Store
ios/
android/
.env*

# SDD scratch (not committed)
.superpowers/

# Expo / native (from create-expo-app template)
web-build/
expo-env.d.ts
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.metro-health-check*
npm-debug.*
yarn-debug.*
yarn-error.*
*.pem
.env*.local
*.tsbuildinfo

# expo-updates code signing — private key must never be committed
keys/

# ...but the PUBLIC code-signing certificate must be committed (build embeds it)
!certs/certificate.pem
